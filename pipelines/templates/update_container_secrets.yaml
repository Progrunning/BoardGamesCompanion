parameters:
  - name: containerAppName
    type: string
  - name: resourceGroupName
    type: string

steps:
  - task: AzureCLI@2
    displayName: Update container secrets
    inputs:
      azureSubscription: $(armServiceConnectionDev)
      scriptType: bash
      scriptLocation: inlineScript
      failOnStandardError: true
      inlineScript: |
        az --version
        az account show

        # Allow installing extension without a prompt
        # Required because of the containerapp command
        az config set extension.use_dynamic_install=yes_without_prompt

        # NOTE: The secret names cannot be longer than 20 characters
        az containerapp secret set -n ${{ parameters.containerAppName }} -g ${{ parameters.resourceGroupName }} --secrets mongodb-connection=$(mongoDbConnectionString)
